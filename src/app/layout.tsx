import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/theme-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FyyndFit - Find Your Fitness",
  description: "AI-powered fitness app for beginners. Scan gym equipment, get personalized workouts, and track your progress.",
  keywords: ["fitness", "gym", "workout", "AI", "equipment scanner", "beginner fitness", "calorie tracker"],
  openGraph: {
    title: "FyyndFit - Find Your Fitness",
    description: "AI-powered fitness app for beginners. Scan gym equipment, get personalized workouts, and track your progress.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
